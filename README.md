####READ ME FIRST

1. Open the file named socket568finalprj in Eclipse\
2. Run server1.java\
3. Run client1.java\
4. Input your command(NOTICE: only BOUNCE, GET and EXIT are valid commands), there must be a space between the command and the content.(eg: BOUNCE message, EXIT code12, GET hello.txt). The console window can show both server output messages and client output messages.\
5. Type EXIT to exit client.